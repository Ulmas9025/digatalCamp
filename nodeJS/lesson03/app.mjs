import fs from 'fs';
import path from 'path';

console.log(fs);


// Замена переменных в шаблоне
//template = template.replace(`{Myname}`, 'Inoyatov Javohir');
//template = template.replace(`{AvtoSayt}`, 'WepPayAuto');

// Запись измененного шаблона в файл index.html
//await fs.promises.writeFile(path.join(__dirname, 'index.html'), template, 'utf-8');
